{host, ...}: let
  inherit (import ../../../hosts/${host}/variables.nix);
in {
  imports = [
    ./binds.nix
    ./env.nix
    ./exec-once.nix
    ./hypridle.nix
    ./hyprland.nix
    ./hyprlock.nix
    ./windowrules.nix
  ];
}
